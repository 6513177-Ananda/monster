class LL{
      NODE*hol;
      int size;
public:
      void add_node(NODE*&);
      void show_all();
      void show_all_dead();
      void attack_all(NODE*&);
      ~LL();
      LL();
};
